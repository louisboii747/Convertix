# Convertix Android app

Native Kotlin + Jetpack Compose application for Android phones and tablets.

Open this `apps/android` directory as the project root in Android Studio.

The project intentionally starts with only the app shell and architectural folders. Convertix API, authentication, conversion, history, and other product features will be added later.
